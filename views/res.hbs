<!DOCTYPE html>
<html>
<head>
    <title>Restaurant</title>
    <style>
        /* Added some basic CSS for table styling */
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Restaurant Details</h1>
    <table>
        <thead>
            <tr>
                <th>Restaurant ID</th>
                <th>Name</th>
                <th>Building</th>
                <th>Street</th>
                <th>Zipcode</th>
                <th>Borough</th>
                <th>Cuisine</th>
            </tr>
        </thead>
        <tbody>
            {{#each data}}
            <tr>
                <td>{{restaurant_id}}</td>
                <td>{{name}}</td>
                <td>{{address.building}}</td>
                <td>{{address.street}}</td>
                <td>{{address.zipcode}}</td>
                <td>{{borough}}</td>
                <td>{{cuisine}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>
